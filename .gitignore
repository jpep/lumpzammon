node_modules/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store

# Note: devanture/firebase-config.js IS committed on purpose. The Firebase
# web API key is public by design (it ends up in any client JS bundle anyway)
# and the skin is served as static files without a build step that could
# inject env vars. Real security lives in the Realtime Database Rules.

screenshots/

WIP.md

# Local Claude Code config (per-user, not for repo)
.claude/

# Stray reference copies (the live assets live in devanture/)
/fond.jpg
/fond[0-9]*.jpg
/fond[0-9]*.png
/p5.min.js
/UsersVDM*
/game/
